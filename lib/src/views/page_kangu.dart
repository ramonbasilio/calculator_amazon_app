import 'package:flutter/material.dart';

class PageKangu extends StatelessWidget {
  const PageKangu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('Envio Próprio - Em desenvolvimento...'),
      ),
    );
  }
}
