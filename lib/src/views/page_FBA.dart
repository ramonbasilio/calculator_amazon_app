import 'package:flutter/material.dart';

class PageFBA extends StatelessWidget {
  const PageFBA({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FBA')
      
      ,),
    );
  }
}